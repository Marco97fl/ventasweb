@model IEnumerable<ventasweb.Models.region>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombreRegion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tarifaEnvio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreRegion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tarifaEnvio.tarifaEnvioId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.regionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.regionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.regionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

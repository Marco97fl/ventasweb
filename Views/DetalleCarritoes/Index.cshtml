@model IEnumerable<ventasweb.Models.DetalleCarrito>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Carrito)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cantidadProd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.precioLinea)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Carrito.lugarEntrega)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.descripcionProd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cantidadProd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.precioLinea)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
